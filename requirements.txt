# ── Data ──────────────────────────────────────────────────────────────────────
yfinance>=0.2.54
pandas>=2.2.0
numpy>=1.26.0
pyarrow>=17.0.0              # Parquet local cache
pandas-market-calendars>=4.4.0
requests>=2.32.0             # SEC EDGAR (El Cazador), Alpaca News (El Analista)

# ── Machine Learning ──────────────────────────────────────────────────────────
scikit-learn>=1.5.0
xgboost>=2.1.0
hmmlearn>=0.3.2              # Hidden Markov Model regime detector (El Conspiranoico)
joblib>=1.3                  # Model serialization (models/*.joblib)
shap>=0.45                   # XAI artifacts for the dashboard

# ── NLP (El Analista, optional: only with --analista) ─────────────────────────
transformers>=4.44.0
torch>=2.3.0

# ── Dashboard API ─────────────────────────────────────────────────────────────
fastapi>=0.111
uvicorn[standard]>=0.29
pydantic>=2.0
slowapi>=0.1.9
matplotlib>=3.9.0            # Static SHAP plots served by the API

# ── Utilities ─────────────────────────────────────────────────────────────────
PyYAML>=6.0.1
tqdm>=4.66.0
python-dotenv>=1.0.0

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=8.3.0
pytest-cov>=5.0.0
