# ──────────────────────────────────────────────────────────────────────────────
# Experimento 9: Juez pass-through vs meta-modelo (Mat + Cazador)
# ──────────────────────────────────────────────────────────────────────────────
# Objetivo: señal directa del Matemático sin meta-modelo logistic (mat+cazador).
# Cazador y Conspiranoico siguen activos vía sus reglas propias (Form 4, veto soft).
#
# Comparar contra meta-modelo (baseline):
#   profiles/exp1_exp3_exp8_combined.yaml  →  judge_v1.mode: meta_model
#
# Uso:
#   python run.py --cazador --conspiranoico --profile profiles/exp9_juez_passthrough.yaml
# ──────────────────────────────────────────────────────────────────────────────

profile:
  name: "exp9_juez_passthrough"
  description: "Exp9: Juez pass-through — solo señal del Matemático"

judge_v1:
  mode: "pass_through"
