# ── Python ────────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.pytest_cache/

# ── Virtual environments / secrets ───────────────────────────────────────────
.venv/
venv/
.env
*.env
!.env.example

# ── Downloaded data (only the ticker universe CSV is versioned) ──────────────
data/cache/
data/raw/
data/processed/

# ── Trained models (registry.json with hashes is versioned) ──────────────────
*.joblib
*.pkl
*.pt

# ── Runtime logs of paper trading ────────────────────────────────────────────
logs/trades/*.jsonl
logs/errors/
logs/positions_current.json

# ── Generated dashboard artifacts ────────────────────────────────────────────
dashboard/api/static/*.png

# ── IDEs / OS ────────────────────────────────────────────────────────────────
.idea/
.vscode/
.DS_Store
Thumbs.db
